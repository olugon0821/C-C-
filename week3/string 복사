#include <stdio.h>
#include <string.h>


int main() {

    char name[30];
    char str[100];

    printf("문자 입력: ");
    fgets(str, sizeof(str),stdin);

    strcpy(name,str);

    printf("%s", name);
    

    return 0;
}
