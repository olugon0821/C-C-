#include <stdio.h>

int main() {
    char str[100];
    int index=0;

    scanf("%s",str);

    while(str[index] != NULL)
        index++;

    for(int i = index - 1; i >= 0; i--){
        printf("%c",str[i]);
    }

    return 0;
}
