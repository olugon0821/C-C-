#include <stdio.h>

int main() {
    int num, sum = 0;

    printf("입력:");
    scanf("%d",&num);

    for(int i=1; i<num; i++){
        if(num % i == 0){
            sum += i;
        }
    }
    if(sum==num){
        printf("Yes \n",num);
    }else{
        printf("No",num);
    }
    return 0;
}
